---
// Cinematic interstitial — full-bleed driving-van video between Service Area
// and Trust. Below-fold autoplay loop, so it lazy-loads via the shared
// LazyVideo component (preload="none" + IntersectionObserver). Hero owns
// the parked golden-hour shot (video-01); this section runs the new
// high-quality driving van (video-04-driving-new.mp4, 1080×1080 H.264
// high profile, 2.66 MB). Replaces the prior video-02-720.mp4 that
// reached its encoding-quality limit per Feroz feedback.

import LazyVideo from '../shared/LazyVideo.astro';
---

<section class="interstitial" aria-hidden="true">
  <LazyVideo
    sources={[
      { src: '/assets/video-04-driving-new.mp4', type: 'video/mp4' },
    ]}
    poster="/assets/video-04-driving-poster.webp"
  />
  <div class="interstitial-caption">
    <div class="left">
      <span>[ —— ]</span>
      <span>An intermission · Vancouver · in motion</span>
    </div>
    <div class="right">A van, <em>always moving</em>.</div>
  </div>
</section>

<style>
  .interstitial {
    position: relative;
    height: 100vh;
    overflow: hidden;
    background: #000;
  }
  .interstitial :global(video) {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }
  .interstitial::after {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(
      180deg,
      rgba(10, 10, 10, 0.4) 0%,
      transparent 25%,
      transparent 75%,
      rgba(10, 10, 10, 0.6) 100%
    );
    pointer-events: none;
  }
  .interstitial-caption {
    position: absolute;
    bottom: 3rem;
    left: 5vw;
    right: 5vw;
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    font-family: var(--mono);
    font-size: 0.7rem;
    letter-spacing: 0.18em;
    color: var(--fg-2);
    text-transform: uppercase;
    z-index: 2;
  }
  .interstitial-caption .left {
    display: flex;
    gap: 1rem;
    align-items: center;
  }
  .interstitial-caption .right {
    font-family: var(--serif);
    font-size: 1rem;
    font-style: italic;
    color: var(--fg);
    letter-spacing: -0.01em;
    text-transform: none;
  }
  .interstitial-caption .right em { color: var(--accent); }

  @media (max-width: 768px) {
    .interstitial { height: 70vh; }
    .interstitial-caption {
      flex-direction: column;
      gap: 0.75rem;
      bottom: 2rem;
    }
  }
</style>
