---
import HeroChoreographed from './HeroChoreographed.tsx';
// v3 hero: single React island. Choreographed scroll on desktop, smart static
// loop on mobile. Reduced-motion: static layout fully revealed. Replaces the
// v2 Astro-stitched hero (HeroPhoneMockup, HeroChips, HeroCounters,
// LiveBookingFeed, BackgroundBeams, Spotlight, Sparkles assembled here).
//
// Those v2 sub-components are intentionally retained on disk for reference
// and rollback. They are simply no longer rendered.
---

<HeroChoreographed client:load />
